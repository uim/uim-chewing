uim-chewing:
  Chewing IM for uim

Requires:
  uim 1.5.0 or later
  libchewing 0.3.2 or later

Install:
  $ ./configure
  $ make
  # make install
  # uim-module-manager --register chewing
  $ uim-pref-gtk &!	# adding Chewing into 'Enabled input method'

Default On/Off key:
  Ctrl-Space
